import React from 'react'
import menuActions from "../data/menu/menuActions";

function menu() {
    /*
    let animation

    if (!rest.menu.state.has(id)) {
        animation = getAnimation(id, {duration, timing, delay});
        menuActions.newMenu()
    } else {
        const menuObj = rest.menu.state.get(id)
        animation = getAnimation(id, {}, menuObj.style)

        // only if entry is not set
        if (!menuObj.entry) animation.transformOrigin = menu.transformOrigin

        CSSHandlerActions.insertRule(id, )
    }
    */
    /*
    return (
    <div id={id} style={animation} {...rest}
        { rest. children }
    </div>
    )
    */
    return <div>Hello from Flux!</div>
}

export default menu;
