import Immutable from 'immutable'

const checkboxes = Immutable.Record({
    id: '',
    type: 'checkboxes',
    typeInput: null,
    //entry: true,
    //entryDirection: null,
    checkLimit: true,
    style: null
})

export default checkboxes
