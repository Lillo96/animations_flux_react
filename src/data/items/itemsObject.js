import Immutable from 'immutable'

const items = Immutable.Record({
    id: '',
    type: 'items',
    typeInput: null,
    checkLimit: true,
    style: null,
    animationCSS: '',
    textInput: '',
    textValue: ''
})

export default items
